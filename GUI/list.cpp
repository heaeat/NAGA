#include "list.h"

void unknownp::setId(std::wstring id) {
	_id = id;
}

void unknownp::setLastuse(std::wstring lastuse) {
	_lastuse = lastuse;
}
void unknownp::setVersion(std::wstring version) {
	_version = version;
}
void unknownp::setCert(std::wstring cert) {
	_cert = cert;
}
void unknownp::setUninstaller(std::wstring uninstaller) {
	_uninstaller = uninstaller;
}